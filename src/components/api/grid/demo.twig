<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Grid</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
<div id="app">
    <demo title="Grid" v-bind:languages="{{ languages|json_encode|escape }}">
        <div slot="content" class="demo-content demo-grid">
            {% for grid in grids %}
                <div class="grid {{ grid.class }}">
                    {% for index, column in grid.columns %}
                        <div class="column c{{ index }}">
                            <div class="widget w{{ index }}">w{{ index }}</div>
                        </div>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>
    </demo>
</div>
<script src="index.js"></script>
</body>
</html>
